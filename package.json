{
  "dependencies": {
    "@types/node": "^20.14.11",
    "dotenv": "^16.4.5",
    "playwright": "^1.50.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "cross-env": "^7.0.3"
  },
  "scripts": {
    "test:ui:env:dev": "cross-env TEST_TYPE=ui test_env=dev npx playwright test tests/ui/",
    "test:api:env:dev": "cross-env TEST_TYPE=api test_env=dev npx playwright test tests/api/",
    "test:all:env:dev": "cross-env test_env=dev npx playwright test"
  }
}
